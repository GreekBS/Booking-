# DEPRECATED LOCATION — do not point Railway Config File Path here.
#
# Railway loads Config-as-Code from the repository root when Root Directory = /.
# The authoritative worker config is:
#
#   /railway.toml
#   /railpack.json
#
# See docs/talos-railway-worker-deploy.md
#
# Kept only as a pointer so old bookmarks do not silently configure nothing.
